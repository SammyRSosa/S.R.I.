# ─── Adquisición ─────────────────────────────────────────────────────────────
beautifulsoup4>=4.12.3
requests>=2.31.0        # TMDB API client
lxml>=5.3.0
curl-cffi>=0.7.0        # TLS fingerprinting (Cloudflare bypass para Letterboxd)

# ─── NLP / Indexación / Búsqueda Vectorial (Corte 2) ──────────────────────────
nltk>=3.8.1
scikit-learn>=1.3.0
sentence-transformers>=3.3.0
faiss-cpu>=1.9.0
numpy>=1.26.0

# ─── API y Generación (RAG / Corte 2) ──────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.10.0
groq>=0.9.0             # LLM Inference (Llama 3 en Groq)
duckduckgo-search>=6.0.0 # Búsqueda Web Fallback
python-dotenv>=1.0.1

